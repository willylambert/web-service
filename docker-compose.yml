services:
  ouiligo:
    image: ${OUILIGO_IMAGE:-ghcr.io/willylambert/ouiligo:latest}
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      ADDR: ":8080"
      GIN_MODE: release
      SNCF_API_TOKEN: "${SNCF_API_TOKEN:-}"
    restart: unless-stopped
